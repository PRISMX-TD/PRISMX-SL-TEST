# 前端环境变量示例 / Frontend env example
# 生产部署（Vercel）时设置为线上后端地址 / Set to the deployed backend URL in production (Vercel)
# 本地开发可留空或不创建此文件（走 vite.config.ts 的代理）/ Leave empty for local dev (uses Vite proxy)
VITE_API_BASE=https://api.prismxsignallab.com
